# Revised Prompt

- Mouse-wheel zoom in the overview should scale the time range correctly, keeping year-to-year spacing expanding rather than snapping back to the minimum and setting the default gap between years to three times its current width.
- When there is enough space, display dates in `MM-YYYY` format.
- The overview rendering should accurately show data density both horizontally and vertically.
- Render points at one-third of their current size (three times smaller).
